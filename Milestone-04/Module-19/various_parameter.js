
function stringLength(str) {
    let size = str.length;
    console.log(size);
}

// stringLength('Bangladesh');

function doubleOrTriple(number, doDoboule) {

    if (doDoboule == true) {
        console.log(number * 2);
    }
    else console.log(number * 3);
}

// doubleOrTriple(5, false);

function lenthOfArray(array) {
    let lenth = array.length;
    console.log(lenth);
}

let age = [45, 6, 75, 3, 5, 34, 53, 23, 45, 34, 7, 23];

lenthOfArray(age);